# Contributing

Contributions are welcome.

## How to help
- Improve the launcher script in [src/launcher.py](src/launcher.py).
- Add better documentation or usage notes.
- Report issues if the bundled executable fails to launch.

## Local checks
Run the tests with:

```bash
python3 -m unittest discover -s tests -v
```
